<UserControl x:Class="AdjustrixWPF.View.UserControls.LabeledComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdjustrixWPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="60*"/>
        </Grid.RowDefinitions>
        <!-- Border -->
        <Border Style="{StaticResource DefaultBorder}" 
                BorderThickness="0 1 0 0"
                Grid.Row="0"
                Grid.RowSpan="2"/>

        <!-- Label -->
        <TextBlock Style="{StaticResource TextBlockTabPanel}"
                   Text="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Bottom"
                   Margin="3"
                   Grid.Row="0"/>

        <!-- ComboBox -->
        <ComboBox Height="22"
                  Margin="5 0 5 0"
                  Background="{DynamicResource HoveredBrush}"
                  Foreground="Transparent"
                  Style="{StaticResource ComboBox}"
                  ItemsSource="{Binding ComboBoxItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding ComboBoxSelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Grid.Row="1"
                  VerticalAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{StaticResource TextBlockTabPanel}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
