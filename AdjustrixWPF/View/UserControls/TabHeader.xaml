<UserControl x:Class="AdjustrixWPF.View.UserControls.TabHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdjustrixWPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" Width="45">
    <Grid>
        <Rectangle Width="45" 
                   Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Fill="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Panel.ZIndex="0"
                   StrokeThickness="0"/>
        <TextBlock Width="45" Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 HorizontalAlignment="Center" VerticalAlignment="Center"
                 Background="Transparent" Foreground="{DynamicResource ForegroundBrush}"
                   TextAlignment="Center" Panel.ZIndex="1"/>
    </Grid>
</UserControl>
