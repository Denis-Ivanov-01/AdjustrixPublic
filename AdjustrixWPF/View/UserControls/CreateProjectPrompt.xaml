<Window x:Class="AdjustrixWPF.View.UserControls.CreateProjectPrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdjustrixWPF.View.UserControls"
        mc:Ignorable="d"
        Title="ManageProjectPrompt" Height="450" Width="800"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    <Border BorderBrush="{DynamicResource ForegroundBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Left"
                       d:Text="Manage Project"
                       Text="Manage Project"
                       FontSize="13"
                       Foreground="{DynamicResource ForegroundBrush}"
                       VerticalAlignment="Center"
                       Margin="10 0 0 0"/>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Border Margin="5" Height="25">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderThickness="0 0 1 0"
                                BorderBrush="{DynamicResource ForegroundBrush}"
                                Width="200">
                            <TextBlock Margin="50 0 50 0"
                                       Text="{Binding LanguageViewModel.ProjectType}"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ComboBox Height="25"
                            ItemsSource="{Binding ProjectTypes}"
                            SelectedItem="{Binding SelectedProjectType}"
                            d:Text="Placeholder name"
                            Style="{StaticResource ComboBox}"
                            Margin="100 0 0 0"
                            x:Name="TypeComboBox"
                            Foreground="{DynamicResource ForegroundBrush}"/>
                    </StackPanel>
                </Border>
                <Border Margin="5" Height="25">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderThickness="0 0 1 0"
                                BorderBrush="{DynamicResource ForegroundBrush}"
                                Width="200">
                            <TextBlock Margin="50 0 50 0"
                                       Text="{Binding LanguageViewModel.ProjectName}"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBox Text="{Binding ProjectProperties.ProjectName}"
                                 d:Text="Placeholder name"
                                 Style="{StaticResource TextBoxStyle1}"
                                 Margin="100 0 0 0"
                                 Tag="Test"
                                 MaxWidth="120"/>
                    </StackPanel>
                </Border>
                <Border Margin="5" Height="25">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderThickness="0 0 1 0"
                                BorderBrush="{DynamicResource ForegroundBrush}"
                                Width="200">
                            <TextBlock Margin="50 0 50 0"
                                       Text="{Binding LanguageViewModel.SiteName}"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBox Text="{Binding ProjectProperties.SiteName}"
                                 d:Text="Placeholder name"
                                 Style="{StaticResource TextBoxStyle1}"
                                 Margin="100 0 0 0"
                                 Tag="Test"
                                 MaxWidth="120"/>
                    </StackPanel>
                </Border>
                <Border Margin="5" Height="25">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderThickness="0 0 1 0"
                                BorderBrush="{DynamicResource ForegroundBrush}"
                                Width="200">
                            <TextBlock Margin="50 0 50 0"
                                       Text="{Binding LanguageViewModel.Client}"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBox Text="{Binding ProjectProperties.Client}"
                                 d:Text="Placeholder name"
                                 Style="{StaticResource TextBoxStyle1}"
                                 Margin="100 0 0 0"
                                 Tag="Test"
                                 MaxWidth="120"/>
                    </StackPanel>
                </Border>
                <Border Margin="5" Height="25">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderThickness="0 0 1 0"
                                BorderBrush="{DynamicResource ForegroundBrush}"
                                Width="200">
                            <TextBlock Margin="50 0 50 0"
                                       Text="{Binding LanguageViewModel.Contractor}"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBox Text="{Binding ProjectProperties.Contractor}"
                                 d:Text="Placeholder name"
                                 Style="{StaticResource TextBoxStyle1}"
                                 Margin="100 0 0 0"
                                 Tag="Test"
                                 MaxWidth="120"/>
                    </StackPanel>
                </Border>
                <Border>
                    <StackPanel Orientation="Horizontal">
                        
                    </StackPanel>
                </Border>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Grid.Row="2"
                        HorizontalAlignment="Center">
                <Button Style="{StaticResource TabButton}"
                        Content="Save"
                        Width="45"
                        Height="30"
                        Margin="0 0 50 0"
                        Click="SaveClick"/>
                <Button Style="{StaticResource TabButton}"
                        Content="Cancel"
                        Width="45"
                        Height="30"
                        Margin="50 0 0 0"
                        Click="CancelClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
