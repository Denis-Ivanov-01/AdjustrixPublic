<Window x:Class="AdjustrixWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdjustrixWPF"
        xmlns:userControls="clr-namespace:AdjustrixWPF.View.UserControls"
        xmlns:compositeControls="clr-namespace:AdjustrixWPF.View.UserControls.Composite"
        mc:Ignorable="d"
        Title="MainWindow" WindowState="Maximized" Height="650" Width="850" WindowStyle="None"
        MouseLeftButtonDown="OnAppWindowWindowOnMouseLeftButtonDown"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Name="MainGrid"
          Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="75*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Image Source="{Binding SystemFileSingleton.LogoPath}"
               Width="45"
               Height="45"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Grid.Row="0"
               Panel.ZIndex="100"/>
        <Border Grid.Row="1" 
                Background="{DynamicResource TabBackgroundBrush}"
                BorderThickness="0">
            <TabControl>
                <TabItem Header="Measurements">
                    <compositeControls:MeasurementsDataGrid DataContext="{Binding DataViewModel}"/>
                </TabItem>
                <TabItem Header="Points">
                    <DataGrid DataContext="{Binding DataViewModel}"
                              AutoGenerateColumns="False"
                              ItemsSource="{Binding KnownBenchmarks}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*"
                                                Binding="{Binding Number}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataContext.LanguageViewModel.Number, 
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                   d:Text="Number"
                                                   Foreground="{DynamicResource ForegroundBrush}"/>
                                    </DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="*"
                                                Binding="{Binding Value, StringFormat=F3}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataContext.LanguageViewModel.Elevation, 
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                   d:Text="Elevation"
                                                   Foreground="{DynamicResource ForegroundBrush}"/>
                                    </DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>
            
        </Border>
        <compositeControls:MainTabControl
            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="_"
                    Style="{DynamicResource TitleBarButtonStyle}" 
                    Name="Minimize" 
                    Click="Minimize_Click"/>
            <Button Content="🗖" 
                    Style="{DynamicResource TitleBarButtonStyle}" 
                    Name="Restore" 
                    Click="Restore_Click"/>
            <Button Content="╳" 
                    Style="{DynamicResource TitleBarButtonStyle}" 
                    Name="Close" 
                    Command="{Binding DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>
